# Chezmoi: gestor único activo de dotfiles relevantes (MCP + SOPS/Age + zsh stack RC files).
# Ver docs/CHEZMOI.md para el alcance vigente.
[source]
    path = "/home/jesus/dotfiles"

[data]
    # SOPS decrypt at apply time; secrets.sops.yaml must exist and be decrypted by sops/age
    # Template uses: {{ (sops "mcp.key") }}
    # Extra data for templates
    storeEtlDir = "{{ .chezmoi.homeDir }}/proyectos/store-etl"

# Obsidian / mcpvault + Filesystem MCP whitelist (override in ~/.config/chezmoi/chezmoi.toml when needed)
[data.ai]
    obsidian_vault_path = "/mnt/c/Users/jesus/Documents/vault_trabajo"

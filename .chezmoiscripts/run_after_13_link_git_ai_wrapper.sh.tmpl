#!/usr/bin/env bash
# Crea symlinks para git-ai-wrapper y git-set-ai-author después de apply.

set -euo pipefail

SCRIPT_DIR="{{ .chezmoi.sourceDir }}"
# shellcheck source=scripts/lib/git-ai-common.sh
source "${SCRIPT_DIR}/scripts/lib/git-ai-common.sh"

git_ai_link_dotfiles_bins "$SCRIPT_DIR" "chezmoi"

exit 0

# AI rules (hub)

Optional agent rules shared across surfaces. Symlinked to `~/.config/ai/rules` on `chezmoi apply`.

Cursor-specific settings remain under `~/.cursor/` and are not managed by this repo.

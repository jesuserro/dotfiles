# AI prompts (hub)

Canonical prompts for agents are published here and symlinked to `~/.config/ai/prompts` on `chezmoi apply`.

Add prompt files under this directory; use `ai-prompt` launchers for vault-backed prompts (see `ai/assets/commands/`).
